@using Transportation_System.Models.Domain
@model Transportation_System.Models.Domain.Bus
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<form asp-controller="Bus" asp-action="Create" method="post" id="createForm" class="ajax-form">
    @Html.AntiForgeryToken()

    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="BusNumber" class="form-label fw-semibold">
                <i class="bi bi-hash me-1 text-primary"></i>Bus Number
            </label>
            <input asp-for="BusNumber" class="form-control" placeholder="e.g. 402" />
            <span asp-validation-for="BusNumber" class="text-danger small"></span>
        </div>
        <div class="col-md-6">
            <label asp-for="BusRouteId" class="form-label fw-semibold">
                <i class="bi bi-signpost-2 me-1 text-primary"></i>Route
            </label>
            <select asp-for="BusRouteId" class="form-select" asp-items="ViewBag.BusRouteId">
                <option value="" disabled selected>-- Select Route --</option>
            </select>
            <span asp-validation-for="BusRouteId" class="text-danger small"></span>
        </div>
    </div>

    <hr class="my-3">

    <h6 class="text-muted text-uppercase small fw-bold mb-2">
        <i class="bi bi-geo-alt me-1"></i>Location
    </h6>
    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="CurrentLatitude" class="form-label fw-semibold">Latitude</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-globe"></i></span>
                <input asp-for="CurrentLatitude" class="form-control" placeholder="00.000000" />
            </div>
            <span asp-validation-for="CurrentLatitude" class="text-danger small"></span>
        </div>
        <div class="col-md-6">
            <label asp-for="CurrentLongitude" class="form-label fw-semibold">Longitude</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-globe"></i></span>
                <input asp-for="CurrentLongitude" class="form-control" placeholder="00.000000" />
            </div>
            <span asp-validation-for="CurrentLongitude" class="text-danger small"></span>
        </div>
    </div>

    <hr class="my-3">

    <h6 class="text-muted text-uppercase small fw-bold mb-2">
        <i class="bi bi-activity me-1"></i>Metrics
    </h6>
    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="Speed" class="form-label fw-semibold">
                <i class="bi bi-speedometer2 me-1 text-success"></i>Speed (km/h)
            </label>
            <input asp-for="Speed" class="form-control" placeholder="0" />
            <span asp-validation-for="Speed" class="text-danger small"></span>
        </div>
        <div class="col-md-6">
            <label asp-for="PassengerCount" class="form-label fw-semibold">
                <i class="bi bi-people me-1 text-success"></i>Passengers
            </label>
            <input asp-for="PassengerCount" class="form-control" placeholder="0" />
            <span asp-validation-for="PassengerCount" class="text-danger small"></span>
        </div>
    </div>

    <hr class="my-3">

    <div class="row g-3">
        <div class="col-md-6">
            <label asp-for="Status" class="form-label fw-semibold">
                <i class="bi bi-circle-fill me-1 text-warning"></i>Status
            </label>
            <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<BusStatus>()">
                <option value="" disabled selected>-- Select Status --</option>
            </select>
            <span asp-validation-for="Status" class="text-danger small"></span>
        </div>
    </div>

    <div class="d-flex justify-content-end gap-2 mt-4 pt-2 border-top">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
            <i class="bi bi-x-lg me-1"></i>Cancel
        </button>
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-lg me-1"></i>Create Bus
        </button>
    </div>
</form>